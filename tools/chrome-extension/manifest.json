{
  "manifest_version": 3,
  "name": "nyan Real Audio Wall connector",
  "version": "0.1.0",
  "description": "Sends tab audio and window positions to the nyan Real Audio Wall OBS source over a local WebSocket, so each browser window sounds from where it is on the virtual screen.",
  "permissions": ["tabs", "system.display", "storage"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js" },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "all_frames": true,
      "run_at": "document_start"
    }
  ],
  "options_page": "options.html"
}
